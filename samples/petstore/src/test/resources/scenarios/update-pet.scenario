# Scenario: Update an existing pet
scenario: Update an existing pet
  when I update the pet
    call ^updatePet
      petId: 1
      body: {"name": "Max Updated", "status": "sold"}
  then the pet is updated
    assert status 200
    assert $.name equals "Max Updated"
    assert $.status equals "sold"
